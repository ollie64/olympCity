akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

services {
  telizeHost = "www.telize.com"
  telizePort = 80
}

project {
  path = "/Users/obaskakov/IdeaProjects/olympCity/"
}